# PROGRESS.md — FUL PathFinder

A running log. Claude Code appends one line after finishing each task: the date, what changed, and anything the user must do next. Newest at the bottom.

Format: `YYYY-MM-DD | Day N | what changed | user action needed (if any)`

---

- 2026-07-27 | Day 1 | Scaffolded Vite + React app, installed maplibre-gl and pmtiles, created the folder structure from docs/ARCHITECTURE.md, rendered a blank full-screen MapLibre map centred on Felele campus (placeholder coords 7.80, 6.74). | Run `npm run dev` and open the printed local URL to see the map; correct the campus centre once survey data gives real coordinates.
- 2026-07-27 | Fix | Downgraded Vite 8 (experimental rolldown-vite) to Vite 7.3.6 because it crashed resolving maplibre-gl's worker file in dev mode. | None, `npm run dev` works clean now.
- 2026-07-28 | Day 3 (done ahead of Day 2, at user's request) | Created the Supabase project directly via the Supabase MCP tools (org: theklvr's Org, project: ful-pathfinder, region eu-west-1, free tier), applied the places/nodes/edges migration with PostGIS and public-read RLS policies, wired src/lib/supabase.js, and seeded 3 clearly-labelled PLACEHOLDER places (not real survey data). Verified reads work and writes are correctly blocked for the anon key. | `.env` with the real project URL and anon/publishable key was created locally in this session (gitignored, not committed) — recreate it from `.env.example` if setting up on another machine. Delete the placeholder places once docs/SURVEY-GUIDE.md data is imported (Day 4). Still need Day 2 (real Protomaps basemap).
- 2026-07-28 | Day 2 | Extracted a Lokoja/Felele region (bbox 6.60,7.75,6.82,7.92; zoom 0-15; ~2.8 MB) from today's Protomaps daily planet build straight via HTTP range requests into public/map/felele.pmtiles, using the go-pmtiles CLI (installed locally, not part of the repo). Wired the PMTiles protocol into MapLibre and styled it with @protomaps/basemaps (light flavor). Also corrected the Felele campus centre in src/map/MapView.jsx from a rough guess (7.80, 6.74 — actually Lokoja town centre) to the Wikipedia/Wikidata-sourced coordinate for the Felele permanent site (7.85944, 6.68361); this is still not survey-verified. Confirmed real roads/buildings/water tile content exists near the campus. | Run `npm run dev` and visually confirm the basemap renders correctly on your machine (couldn't screenshot in this environment). The Wikipedia-sourced campus centre is ~9 km from the old placeholder — flag if that looks wrong once you're on the ground.
- 2026-07-28 | Survey import | User supplied the real campus survey as a Google My Maps KML export (Assets/FUL PathFinder Survey.kml). Converted to data/places.csv (47 places) and data/network.geojson (133 nodes, 137 edges) via scripts/convert-survey-kml.mjs. Corrected on user's instruction: Girls/Boys hostel category landmark -> hostel; Line 26 was missing the vertex where Line 27 actually touches it (~0.15m off) — inserted it; Line 30 fell ~20m short of the network — extended it to the nearest junction (Line 36). Network is now fully connected (133/133 nodes reachable from the gate). One pin still has no name/category and is excluded from places.csv — the user doesn't know what it is until they're next on campus. | None immediately — when that unnamed pin gets identified, add it to Assets/FUL PathFinder Survey.kml and re-run `npm run convert-survey`.
- 2026-07-28 | Day 4 | Wrote supabase/seed/import_places.mjs (reads data/places.csv, requires SUPABASE_SERVICE_ROLE_KEY to run standalone) and loaded the real 47 places into the live `places` table directly via the Supabase MCP tools (replacing the 3 Day-3 placeholder rows). Added src/data/categories.js (category -> colour map) and src/data/places.js (fetchPlaces query), and wired src/map/MapView.jsx to render every place as a category-coloured marker. | Run `npm run dev` and visually confirm 47 coloured markers appear on the map.
- 2026-07-28 | Day 5 | Added src/components/PlaceCard.jsx (name, category, description, photo if present, disabled Directions button pending Day 9) and src/components/CategoryFilter.jsx (toggleable chip per category). Marker click opens the card; unchecking a category hides its markers. | Run `npm run dev`, tap a marker to see its card, toggle a category chip to confirm filtering.
- 2026-07-28 | Day 6 | Added src/components/SearchBar.jsx, matching on place name and aliases (case-insensitive substring), showing up to 8 results. MapView now exposes an imperative flyTo(place) via forwardRef/useImperativeHandle; selecting a search result flies the map there and opens its PlaceCard. | Run `npm run dev` and search a nickname (e.g. "LT", "SUB"-style aliases) to confirm it flies to the right place. Phase B (Data and display) is now complete; Phase C (routing) starts at Day 7, which needs data/network.geojson loaded into nodes/edges — not yet done.
- 2026-07-28 | Fix (blank white screen) | User reported the app rendering completely blank in real Chrome. Root cause: src/map/MapView.jsx line 21 did `const markersRef = useRef(new Map())` intending the native JS Map collection, but `Map` was shadowed by maplibre-gl's `Map` class imported on line 2 — so it was calling `new maplibregl.Map()` with zero arguments, which threw inside maplibre-gl's constructor and, with no error boundary, blanked the entire React tree silently. Fixed by aliasing the import (`Map as MaplibreMap`). Also added a minimal ErrorBoundary in src/main.jsx so any future render crash shows a visible stack trace instead of a silent blank page. Verified by driving headless Chrome against the actual running dev server and reading the rendered DOM (not just curl, which never executes JS) — confirmed all 47 markers, the search bar, and the category filter now render with zero errors. | Refresh the browser tab — should now show the map with 47 coloured markers.
- 2026-07-28 | Day 7 | Wrote supabase/seed/import_network.mjs (reads data/network.geojson, computes distance_m via haversine; note nodes.id is `generated always as identity` so the REST API can't set explicit ids — the script inserts without ids and remaps edges by insertion order). Live-loaded 133 nodes and 139 edges into Supabase via the MCP tools, using raw SQL with `overriding system value` to keep node ids identical to network.geojson's own ids (easier to cross-reference for debugging). Computed and set nearest_node_id for all 47 places (max distance to nearest node: 141m, for School Stadium — a real gap in the survey network, not a bug). Added src/data/network.js (fetchNodes/fetchEdges) and a faint debug line layer in MapView showing the path network. Verified visually: built a small reusable CDP screenshot/eval tool (scratchpad, not committed) to actually drive headless Chrome and read back the live map instance's internal state (style layers, sources, errors) plus real screenshots — confirmed the basemap, 47 markers, and the debug path network all render correctly together over real OSM roads/buildings near the campus. | Run `npm run dev` and pan around the campus — faint gray lines should trace the surveyed footpaths. Phase C (routing) continues at Day 8 (A* implementation).
- 2026-07-28 | Day 8 | Implemented src/routing/haversine.js, graph.js (buildGraph: nodes+edges -> adjacency list + coords map), and astar.js (hand-rolled binary min-heap, A* with a pluggable heuristic, dijkstra() as A* with a zero heuristic, haversineHeuristic() helper). Added src/routing/astar.test.js using Node's built-in test runner (`npm test`, no new dependency) — a hand-checkable graph with a decoy same-hop-count route proves the search optimizes true distance, not hop count, and that Dijkstra agrees with A*. Also sanity-checked both algorithms against the real 133-node campus graph (School Gate -> Library: 1861m over 21 hops; -> Boys hostel: 2181m over 22 hops; Dijkstra matched A* on both). | None — `npm test` passes. Phase C continues at Day 9 (wire the Directions button end to end).
- 2026-07-28 | Day 9 | Enabled the PlaceCard Directions button; added src/components/DirectionsPanel.jsx (From dropdown of all places, defaulting to School Gate; To is fixed to whichever place's Directions button was clicked; shows distance and ~1.3 m/s walking time). App.jsx builds the routing graph once from nodes/edges (useMemo) and reruns A* whenever origin/destination change, snapping both to their nearest_node_id. MapView gained a `route` prop that draws/updates a highlighted line via a GeoJSON source+layer. Verified by driving the actual click flow through Chrome DevTools Protocol (marker click -> PlaceCard -> Directions -> panel) against the live dev server: "School Gate -> Admin Building" correctly showed 1.5 km / 20 min walk with the route line rendered along the real surveyed path in a screenshot. | None — try it in the browser: click any marker, then Directions. Phase C finishes at Day 10 (turn-by-turn steps).
- 2026-07-28 | Day 10 | Added src/routing/steps.js: walks the routed node path, computing bearing between consecutive nodes and splitting into "legs" wherever the bearing changes by more than 25°, calling out the nearest place within 50m of each turn point (each landmark used once, never repeating the origin/destination). Wired into DirectionsPanel as a numbered step list below the distance/time summary. Verified against the real campus graph both via a standalone script and via the live browser (CDP): "School Gate -> Library" produced "Turn right after 1.5 km, past Bus Stop. Turn left after 121 m. Turn right after 80 m, past Faculty of Computing. Arrive at Library." — genuinely readable, and a straight route (School Gate -> Admin Building) correctly produced zero turn steps rather than a false one. | None. **Phase C (Routing) is now complete (Days 7-10) — the graded centerpiece works end to end: search or click a place, hit Directions, get a real A*-routed path with distance, walk time, and turn-by-turn steps.** Phase D (location and polish: Days 11-15) is next, starting with live geolocation.
- 2026-07-28 | Day 11 (expanded into full live navigation, at user's request — "look and function like AMAP", not a static one-shot direction list) | Visual redesign: new palette grounded in Federal University Lokoja's actual crest colours (Navy `#0B2545`, Sky `#4FA8E0`) plus a deliberate amber "Confluence" accent (`#F2A93B`) representing the laterite footpaths this app routes over — researched via web search rather than guessed, since this is literally the university's own app. Applied across all existing UI (search bar, category chips, place card, directions panel), not just the new nav surfaces. Built src/location/useLiveLocation.js (watchPosition, with a draggable puck marker in MapView for manual GPS-drift correction — the puck is a compact echo of the crest's concentric shields: navy ring, sky heading cone, amber centre). Built real live navigation: App.jsx recomputes the A* route from the walker's live GPS position on every update (src/routing/graph.js gained nearestNodeId()) — this is inherently self-correcting/rerouting with no separate "off-route" check needed. src/routing/steps.js now returns {text, kind, at} objects (kind drives a direction icon; at is the coordinate used for live distance countdown). New src/components/NavBanner.jsx (top instruction banner, AMAP/Google-Maps convention — next instruction belongs near the eyes, not the thumbs) and NavBottomBar.jsx (remaining distance/ETA + End button). Voice guidance via the Web Speech API (SpeechSynthesisUtterance), speaking each instruction once when it becomes current, with a mute toggle. Arrival detection (within 15m) shows "You've arrived" and auto-exits nav mode after 4s. | **Verification and an important process fix**: discovered mid-session that all of this session's earlier "headless" Chrome testing (Days 7-10) had been silently redirecting into the user's real, already-running Chrome via Windows' default-profile single-instance behaviour — `--headless` was being ignored, and CDP geolocation grants failed because of it. Fixed by launching with an isolated `--user-data-dir`, after which `Emulation.setGeolocationOverride` worked correctly. Re-verified the full flow end to end by simulating a walk (search Library -> Directions -> Start navigation -> stepping the simulated GPS position toward the destination): distance-to-turn counted down accurately at every step (1.8km -> 1.5km -> 1.2km -> 877m -> 602m -> 309m -> 0m), the bottom bar's ETA/remaining distance tracked in lockstep, the instruction correctly simplified as the turn got close, arrival was detected precisely at the destination, and the app auto-returned to normal browsing 4s later. Screenshots confirm the full visual design renders correctly (navy banner, pulsing sky-blue puck on the destination building, monospace ETA in the bottom bar). | **The user should check their own Chrome window for stray tabs** — since testing wasn't properly isolated until partway through this session, some of the automated browser testing for Days 7-10 likely ran in a real, visible tab in their actual browser rather than a hidden headless instance. All testing from Day 11 onward uses an isolated profile and won't touch their real browser. Try live navigation in `npm run dev`: search a place, hit Directions, then "Start navigation" (grant location permission when prompted). Days 12-15 (mobile polish audit, offline support, deploy, field-test) are still open.
- 2026-07-28 | Day 12 | Mobile audit via CDP at a 390x844 (iPhone 12/13-sized) viewport with touch emulation found two real issues: several tap targets under the 44px accessibility guideline (category chips were 35px, search input 41px), and the bottom cards read as floating panels rather than true bottom sheets. Fixed: bumped category chips/search input/close buttons/voice toggle/End button to 44-48px; added a drag handle (src/hooks/useSwipeToDismiss.js) to PlaceCard and DirectionsPanel with a real swipe-down-to-close gesture (touch events, not just a decorative bar); added a scroll-fade hint on the category chip row so its horizontal scrollability is visually obvious. Confirmed zero horizontal overflow at the phone viewport. | None. Verified via CDP: dispatched real synthetic touch events for a 150px downward swipe on the handle and confirmed the card actually closes. MapLibre's own zoom/compass buttons stay small (29px, library-default) — left alone since pinch-to-zoom is the primary touch gesture, not those buttons. Day 13 (offline/service worker) is next.
- 2026-07-29 | Day 13 | Added public/sw.js, a hand-rolled service worker (no framework/plugin dependency): the app shell uses stale-while-revalidate, Supabase REST calls (places/nodes/edges) use network-first with a cache fallback, and the pmtiles basemap is precached whole on install and served to the pmtiles library's HTTP Range requests by manually slicing the cached ArrayBuffer into 206 Partial Content responses (the Cache API doesn't do this on its own). Registered only in production builds (main.jsx checks import.meta.env.PROD) to avoid caching the dev server's ever-changing module URLs. Added LoadingScreen and ErrorBanner (with Retry) components, plus a 15s client-side timeout on the initial data fetch. | **Two real bugs found and fixed via testing, not just written and assumed correct**: (1) a genuinely offline reload with nothing cached showed neither the loading screen nor an error — turned out to be a correct, unavoidable PWA constraint (a device that's never once been online can't load any web app on its first visit), not a bug; but testing a more realistic "Supabase is down while the app shell is already cached" scenario surfaced a real one: (2) a blocked/hanging request left the loading screen spinning forever with no way out — fixed with the 15s timeout — and (3) the error banner visually overlapped the search bar (both absolutely positioned at the same spot) — fixed by making it a normal flex child stacked above the search bar instead. Verified via CDP with a full production build (`npm run preview`) and Chrome's Network domain: cut network entirely after first load -> app still shows the map, all 47 markers, and category filters purely from cache; blocked only the Supabase host -> loading screen correctly gives way to the error banner after 15s, and Retry correctly recovers once unblocked. | None. `npm run build && npm run preview` to try the offline behaviour yourself (DevTools → Application → Service Workers, or Network → Offline). Day 14 (deploy) is next.
- 2026-07-29 | Day 14 | Pushed the repo to github.com/theklvr/ful-pathfinder and deployed via Vercel's git integration (KLVR team). First build failed ("vite: command not found", no install step logged) because the repo's default branch (`main`, auto-created by GitHub with a LICENSE + 1-line README) had never received our actual commit history — everything had been pushed to `master` instead. Merged `master` into `main` (one conflict, on README.md, resolved in favour of the real one) and pushed; the resulting build succeeded. Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Vercel's project env vars (values given to the user directly, safe to expose client-side — Supabase RLS controls actual access). | **Two things the user needs to act on**: (1) the GitHub repo came back `public`, not `private` as originally requested — worth checking github.com/theklvr/ful-pathfinder/settings if that matters. (2) **Vercel's Deployment Protection (SSO) is ON**, so https://ful-pathfinder-klvr.vercel.app currently redirects every visitor to a Vercel login — no student can open it as-is. Turn it off in the KLVR Vercel project's Settings → Deployment Protection. Everything else was verified working on the *live* production URL itself (via a temporary bypass link, not just locally): title, 47 markers, service worker active, search, and a full Directions flow (School Gate -> Library, matching distance/steps from local testing exactly) all confirmed. Local git default branch is now `main` (matches GitHub/Vercel) — commit there going forward; `master` still exists but is no longer the active branch. Real on-campus phone/network testing is still outstanding (Day 15). Day 15 (README, demo script) is next — the "fix issues found in real campus testing" part of Day 15 needs the user to actually go test on campus first.
- 2026-07-29 | Critical fix + AMAP-style visual pass | User reported the deployed app rendering as a completely flat gray screen with only colour dots (no roads, buildings, or basemap at all) and asked for an AMAP/Google-Maps level visual upgrade, sharing real phone screenshots of Google Maps' icons, map-type switcher, and walking-directions UI as reference. **Root cause found (this was a real, deterministic bug, not a "second load" or network flakiness issue as first suspected — an earlier hypothesis about the service worker's hand-rolled pmtiles byte-slicing was ruled out by direct testing and discarded)**: MapLibre GL computes its worker script's URL at runtime relative to its own bundled chunk's `import.meta.url`, using a pattern (`e.endsWith('-dev.mjs') ? ... : 'maplibre-gl-worker.mjs'`) that Vite's static analyzer can't detect — so `vite build` never emitted or copied `maplibre-gl-worker.mjs` into `dist/`. Every production load 404'd fetching it, silently falling back to Vite preview's HTML SPA-fallback response ("Failed to load module script: non-JavaScript MIME type"), the worker never started, and vector tiles never got parsed into renderable geometry — only the plain DOM markers ever painted. **Fix**: copied `maplibre-gl-worker.mjs` and its `maplibre-gl-shared.mjs` dependency from `node_modules/maplibre-gl/dist/` into `public/maplibre/` (committed as static assets, precached by the service worker) and call `setWorkerUrl('/maplibre/maplibre-gl-worker.mjs')` before creating the map. Verified fixed across 4 consecutive fresh loads on a 390x844 mobile viewport, both in Vite preview and with the service worker fully disabled as a control. Also simplified `public/sw.js`'s pmtiles handling from always-slice-a-cached-buffer to network-first-with-cache-fallback (the old approach had an unrelated intermittent race; the real server already serves Range requests natively when online). **Visual pass**: replaced plain colour dots with Google-Maps-style pin markers (CSS teardrop shape, anchor:'bottom') carrying hand-drawn per-category line-icon SVGs (cap for faculty, bed for hostel, briefcase for admin, coffee cup for eatery, banknote for ATM, flag for landmark, wrench for service, medical cross for health, activity line for sport — see `src/data/categoryIcons.js`); made the surveyed path network an actual visible layer (white casing + dashed amber line, up from a near-invisible 0.2-opacity debug line); added a route-line casing for a real-nav-app look; added a light/dark map style toggle button using `@protomaps/basemaps`' named flavors (our free equivalent of Google's map-type switcher — satellite imagery needs a paid provider, ruled out by the free-tools-only constraint). The style toggle initially silently failed to restore the route/path layers after switching (MapLibre's default style-diffing was stalling with custom layers already present and never firing `style.load`); fixed by forcing `{ diff: false }` on `setStyle()`. | None to act on for the fix/visual work — verified and pushed. **Photos are not wired up**: real survey photos exist in `Assets/images/` (27 files, ~104MB, filenames match place names closely) but two things blocked finishing this: (1) no image compression tool was available in this environment without adding a new npm dependency (sharp/imagemagick) — needs the user's go-ahead first per CLAUDE.md's "ask before adding any dependency" rule; (2) the `photo_url` column can't be written — the anon key's RLS policy only allows public *read*, and the Supabase MCP connection needs re-authorization (`claude mcp` or `/mcp` in an interactive session) to do it the way earlier sessions did. Re-authorize Supabase MCP and say go-ahead on a compression tool to unblock this.
- 2026-08-03 | OSM data pipeline (planned with the user, zero new dependencies) | User has personally mapped Felele campus (buildings, footpaths, names) directly on openstreetmap.org via the iD editor, traced against satellite imagery, and saved/uploaded the edits — this is now the ongoing primary source of campus data, merging with (not replacing) the existing 47-place/133-node/139-edge GPS survey. Added `scripts/convert-survey-osm.mjs` (reads a raw Overpass API export — NOT Overpass Turbo's "GeoJSON" export, which discards the OSM node ids that identify shared path junctions — maps OSM tags to the app's 9 fixed categories with a flag-don't-guess fallback, converts footway/path/steps/service/etc. ways into the node/edge shape, with a tight 2m defensive snap for iD editing mistakes) and `scripts/merge-survey.mjs` (folds the OSM-derived staging files into the canonical `data/places.csv`/`data/network.geojson`, detecting duplicate places by name+proximity, merging matching path junctions across the two independently-collected datasets within 12m, and running a connectivity check from School Gate that hard-fails if any place ends up in a disconnected component). Added `data/place-overrides.csv` so manual Supabase edits like `photo_url` survive future re-merges. Smoke-tested both scripts end to end against a synthetic Overpass fixture and the real canonical data (git-restored afterward, no synthetic data left in the repo) — correctly caught a synthetic duplicate "School Gate" node/place, merged one synthetic junction into an existing node, added new nodes/places, and reported a single connected component. `npm test` still passes (routing untouched). No changes to the Supabase schema, `src/routing/*`, `src/map/*`, or the two existing seed scripts. | **User action needed:** run the Overpass query in `docs/SURVEY-GUIDE.md`'s new "Keeping OpenStreetMap in sync" section at overpass-turbo.eu, use **Export → "raw data"**, save as `Assets/felele-osm-export.json`, then run `npm run convert-survey-osm` and `npm run merge-survey`, reviewing each report before running the existing seed scripts. This hasn't been run against the real OSM export yet — only against synthetic test data.
- 2026-08-04 | OSM merge run for real + Phase 1 visual overhaul | User provided the real Overpass raw-data export (`Assets/felele-osm-export.json`, 3088 elements). Ran `convert-survey-osm` then `merge-survey`: network grew from 133 nodes/139 edges to 1883 nodes/1945 edges (~46km of real traced path), places grew from 47 to 48 (19 OSM entries correctly recognized as existing places and skipped, 3 held back as "same spot, different name" for manual review — LT A/Lecture Theatre A, LT B/Lecture Theatre B, Central Mosque/School Mosque). Connectivity check passed (no place in a disconnected component); **not yet seeded into Supabase — `SUPABASE_SERVICE_ROLE_KEY` is missing from `.env`.** Then built the Phase 1 visual pass agreed on with the user (matching Google Maps reference screenshots): persistent name labels next to every map marker (`src/map/MapView.jsx`, `.place-marker-wrap`/`.place-marker-label`); category chips changed from multi-select filter toggles to single-select (`src/components/CategoryFilter.jsx`, `src/App.jsx`) that both filter the map and open a new bottom-sheet list view (`src/components/PlaceList.jsx`) showing every place in that category with a thumbnail/icon, name, description, Directions button, and Share/Call/More actions; a shared `PlaceActions` component (`src/components/PlaceActions.jsx`, `src/utils/share.js`) adds Share (native Web Share API, clipboard fallback) and a More menu (copy coordinates, open in Google Maps) to both the list rows and the place detail card, Call only rendering if a `phone` field is ever present (schema doesn't have one yet); a new "Me" button (`.me-button` in MapView) recenters the map on the browser's live location and shows the heading puck, independent of full turn-by-turn nav mode; the search bar gained a microphone button for voice search via the browser's built-in SpeechRecognition API (renders only if the browser supports it); and a small account icon next to search shows a dismissible "Accounts are coming soon" notice — honest placeholder, not a fake working login, since real accounts are Phase 3. Verified in a real headless-Chromium browser at a 390x844 mobile viewport (Playwright, installed only in the session's scratchpad dir, not added to the project's dependencies): all six pieces worked with zero console errors, confirmed via both screenshots and direct DOM assertions (chip active-state, puck presence, notice visibility). `npm run lint` and `npm test` both pass. | **Two things needed:** (1) add `SUPABASE_SERVICE_ROLE_KEY` to `.env` (from Supabase dashboard → Settings → API) so the merged OSM data can actually be seeded into the live database — ask and I'll run the seed scripts; (2) look at the app in a real browser (`npm run dev`) to react to the new visual pass before we move on to Phase 2.
- 2026-08-04 | Fixed two real gaps user flagged after looking at the Phase 1 pass | (1) **Building shapes were invisible**: `public/map/felele.pmtiles` is a frozen basemap extract from before the campus was mapped in OSM, and Protomaps' builds lag live OSM by days, so the traced buildings would've stayed invisible regardless of how much gets mapped. Fixed by drawing them ourselves: `scripts/convert-survey-osm.mjs` now also extracts every building way's full polygon ring (not just its centroid) from the same Overpass export into `public/map/campus-buildings.geojson` (152 footprints) as a separate visual-only output — doesn't touch the places/network pipeline or Supabase. `src/map/MapView.jsx` fetches and renders it as a fill+outline layer, inserted below the path-network layer. (2) **Place card/list "modal" didn't match the Google Maps reference images**: replaced the old full-width text button + separate pill row with a single icon-over-label action row (Directions/Call/Share/More, `src/components/PlaceActions.jsx`), matching Google Maps' actual card convention — used identically in both the place detail card and the category list rows now. Verified in a real headless browser: building polygons render correctly under the markers, action row renders and the More menu still opens, zero console errors, lint/tests still pass. | Look at it again in a real browser and react — this was two rounds of "doesn't match the reference" feedback, want to catch anything still off before continuing to Phase 2.
- 2026-08-04 | Phase 2: free-pick Directions | Added a blue Directions FAB on the map (`.directions-fab` in `src/map/MapView.jsx`) that opens the directions panel with nothing preselected — previously the only way in was tapping a specific place's Directions button. Both "From" and "To" are now typeahead fields (new `src/components/PlaceSelectField.jsx`, same name/alias matching as the search bar) instead of a plain dropdown with a fixed destination, so either side can be freely searched and changed. Added a reverse/swap button between them (disabled until both are set) that swaps origin and destination in one tap. `src/App.jsx` gained a `showDirections` flag independent of `destinationPlace` so the panel can open empty; `DirectionsPanel` now handles all three states cleanly (no destination yet / no origin yet / route found / no route exists) instead of assuming a destination is always present. Verified in a real headless browser: opened directions with nothing picked, typed "Faculty of Law" into To, selected it, got a real routed distance/time/steps, hit reverse and confirmed From/To actually swapped (School Gate → Faculty of Law became Faculty of Law → School Gate) — zero console errors, lint/tests pass. | Try it in `npm run dev`: tap the new blue directions button on the map (not a place first) and pick both ends yourself.
- 2026-08-04 | Phase 3: real accounts | Email/password auth needed no dashboard toggle — Supabase turns it on by default for every project. Added `supabase/migrations/20260804090000_accounts_favorites_reviews.sql` (new `favorites` and `reviews` tables linked to `auth.users`, RLS: reviews public-read/owner-write, favorites fully private to the owner) — **not yet applied to the live database**, no Supabase MCP connection this session to run it directly. Added `src/lib/auth.js` (`useAuth()` hook wrapping `supabase.auth`) and `src/components/AccountPanel.jsx` (sign in/up tabs, real form, error/confirmation messaging) — replaces the old "Accounts are coming soon" placeholder from Phase 1 with the real thing. Wired into `src/App.jsx`/the account button in `src/index.css`. Verified the UI itself in a real browser (tab switching, form rendering, no console errors) but **did not submit an actual sign-up** — that would create a real row in the live Supabase auth users table, and this session has no way to clean that up afterward, so it needs a real decision from the user rather than a test account I create silently. | **User action needed:** open the Supabase dashboard → SQL Editor, paste in `supabase/migrations/20260804090000_accounts_favorites_reviews.sql`, and run it — that's what actually creates the `favorites`/`reviews` tables live. After that, try signing up for a real account in the app yourself to confirm the end-to-end flow (favorites/reviews UI itself isn't built yet, this pass is just making accounts real).
- 2026-08-04 | Phase 3 continued: favorites + reviews UI | User confirmed the migration was applied. Built the two features the new tables were for: (1) a Save/bookmark button on the place card (`src/components/PlaceCard.jsx`, `src/data/favorites.js`) that fills amber when saved, prompts sign-in if you're not logged in rather than silently failing; (2) real star ratings and reviews (`src/components/PlaceReviews.jsx`, `src/components/Stars.jsx`, `src/data/reviews.js`) — a 5-star picker plus optional comment, upserts so re-rating a place you already rated updates it instead of erroring on the table's unique constraint, shows "No ratings yet" honestly when a place has none rather than faking a number, and the category list view now shows each place's real average rating (one bulk query per category, not one query per place). Verified in a real browser signed OUT (to avoid writing a test account into the live database): rating/favorite data loads without errors (reviews are public-read per the RLS policy, favorites correctly returns empty for no session), clicking Save or a star while logged out correctly opens the sign-in panel instead of failing silently. Lint/tests pass. | **Still not tested signed IN** — same reasoning as last entry, submitting a real sign-up from this session would leave a permanent account in the live database with no way to clean it up. Sign up for real, then try saving a place and leaving a review yourself to confirm the write path actually works end to end.
