# PROGRESS.md — FUL PathFinder

A running log. Claude Code appends one line after finishing each task: the date, what changed, and anything the user must do next. Newest at the bottom.

Format: `YYYY-MM-DD | Day N | what changed | user action needed (if any)`

---

- 2026-07-27 | Day 1 | Scaffolded Vite + React app, installed maplibre-gl and pmtiles, created the folder structure from docs/ARCHITECTURE.md, rendered a blank full-screen MapLibre map centred on Felele campus (placeholder coords 7.80, 6.74). | Run `npm run dev` and open the printed local URL to see the map; correct the campus centre once survey data gives real coordinates.
- 2026-07-27 | Fix | Downgraded Vite 8 (experimental rolldown-vite) to Vite 7.3.6 because it crashed resolving maplibre-gl's worker file in dev mode. | None, `npm run dev` works clean now.
- 2026-07-28 | Day 3 (done ahead of Day 2, at user's request) | Created the Supabase project directly via the Supabase MCP tools (org: theklvr's Org, project: ful-pathfinder, region eu-west-1, free tier), applied the places/nodes/edges migration with PostGIS and public-read RLS policies, wired src/lib/supabase.js, and seeded 3 clearly-labelled PLACEHOLDER places (not real survey data). Verified reads work and writes are correctly blocked for the anon key. | `.env` with the real project URL and anon/publishable key was created locally in this session (gitignored, not committed) — recreate it from `.env.example` if setting up on another machine. Delete the placeholder places once docs/SURVEY-GUIDE.md data is imported (Day 4). Still need Day 2 (real Protomaps basemap).
- 2026-07-28 | Day 2 | Extracted a Lokoja/Felele region (bbox 6.60,7.75,6.82,7.92; zoom 0-15; ~2.8 MB) from today's Protomaps daily planet build straight via HTTP range requests into public/map/felele.pmtiles, using the go-pmtiles CLI (installed locally, not part of the repo). Wired the PMTiles protocol into MapLibre and styled it with @protomaps/basemaps (light flavor). Also corrected the Felele campus centre in src/map/MapView.jsx from a rough guess (7.80, 6.74 — actually Lokoja town centre) to the Wikipedia/Wikidata-sourced coordinate for the Felele permanent site (7.85944, 6.68361); this is still not survey-verified. Confirmed real roads/buildings/water tile content exists near the campus. | Run `npm run dev` and visually confirm the basemap renders correctly on your machine (couldn't screenshot in this environment). The Wikipedia-sourced campus centre is ~9 km from the old placeholder — flag if that looks wrong once you're on the ground.
- 2026-07-28 | Survey import | User supplied the real campus survey as a Google My Maps KML export (Assets/FUL PathFinder Survey.kml). Converted to data/places.csv (47 places) and data/network.geojson (133 nodes, 137 edges) via scripts/convert-survey-kml.mjs. Corrected on user's instruction: Girls/Boys hostel category landmark -> hostel; Line 26 was missing the vertex where Line 27 actually touches it (~0.15m off) — inserted it; Line 30 fell ~20m short of the network — extended it to the nearest junction (Line 36). Network is now fully connected (133/133 nodes reachable from the gate). One pin still has no name/category and is excluded from places.csv — the user doesn't know what it is until they're next on campus. | None immediately — when that unnamed pin gets identified, add it to Assets/FUL PathFinder Survey.kml and re-run `npm run convert-survey`.
- 2026-07-28 | Day 4 | Wrote supabase/seed/import_places.mjs (reads data/places.csv, requires SUPABASE_SERVICE_ROLE_KEY to run standalone) and loaded the real 47 places into the live `places` table directly via the Supabase MCP tools (replacing the 3 Day-3 placeholder rows). Added src/data/categories.js (category -> colour map) and src/data/places.js (fetchPlaces query), and wired src/map/MapView.jsx to render every place as a category-coloured marker. | Run `npm run dev` and visually confirm 47 coloured markers appear on the map.
- 2026-07-28 | Day 5 | Added src/components/PlaceCard.jsx (name, category, description, photo if present, disabled Directions button pending Day 9) and src/components/CategoryFilter.jsx (toggleable chip per category). Marker click opens the card; unchecking a category hides its markers. | Run `npm run dev`, tap a marker to see its card, toggle a category chip to confirm filtering.
- 2026-07-28 | Day 6 | Added src/components/SearchBar.jsx, matching on place name and aliases (case-insensitive substring), showing up to 8 results. MapView now exposes an imperative flyTo(place) via forwardRef/useImperativeHandle; selecting a search result flies the map there and opens its PlaceCard. | Run `npm run dev` and search a nickname (e.g. "LT", "SUB"-style aliases) to confirm it flies to the right place. Phase B (Data and display) is now complete; Phase C (routing) starts at Day 7, which needs data/network.geojson loaded into nodes/edges — not yet done.
- 2026-07-28 | Fix (blank white screen) | User reported the app rendering completely blank in real Chrome. Root cause: src/map/MapView.jsx line 21 did `const markersRef = useRef(new Map())` intending the native JS Map collection, but `Map` was shadowed by maplibre-gl's `Map` class imported on line 2 — so it was calling `new maplibregl.Map()` with zero arguments, which threw inside maplibre-gl's constructor and, with no error boundary, blanked the entire React tree silently. Fixed by aliasing the import (`Map as MaplibreMap`). Also added a minimal ErrorBoundary in src/main.jsx so any future render crash shows a visible stack trace instead of a silent blank page. Verified by driving headless Chrome against the actual running dev server and reading the rendered DOM (not just curl, which never executes JS) — confirmed all 47 markers, the search bar, and the category filter now render with zero errors. | Refresh the browser tab — should now show the map with 47 coloured markers.
- 2026-07-28 | Day 7 | Wrote supabase/seed/import_network.mjs (reads data/network.geojson, computes distance_m via haversine; note nodes.id is `generated always as identity` so the REST API can't set explicit ids — the script inserts without ids and remaps edges by insertion order). Live-loaded 133 nodes and 139 edges into Supabase via the MCP tools, using raw SQL with `overriding system value` to keep node ids identical to network.geojson's own ids (easier to cross-reference for debugging). Computed and set nearest_node_id for all 47 places (max distance to nearest node: 141m, for School Stadium — a real gap in the survey network, not a bug). Added src/data/network.js (fetchNodes/fetchEdges) and a faint debug line layer in MapView showing the path network. Verified visually: built a small reusable CDP screenshot/eval tool (scratchpad, not committed) to actually drive headless Chrome and read back the live map instance's internal state (style layers, sources, errors) plus real screenshots — confirmed the basemap, 47 markers, and the debug path network all render correctly together over real OSM roads/buildings near the campus. | Run `npm run dev` and pan around the campus — faint gray lines should trace the surveyed footpaths. Phase C (routing) continues at Day 8 (A* implementation).
- 2026-07-28 | Day 8 | Implemented src/routing/haversine.js, graph.js (buildGraph: nodes+edges -> adjacency list + coords map), and astar.js (hand-rolled binary min-heap, A* with a pluggable heuristic, dijkstra() as A* with a zero heuristic, haversineHeuristic() helper). Added src/routing/astar.test.js using Node's built-in test runner (`npm test`, no new dependency) — a hand-checkable graph with a decoy same-hop-count route proves the search optimizes true distance, not hop count, and that Dijkstra agrees with A*. Also sanity-checked both algorithms against the real 133-node campus graph (School Gate -> Library: 1861m over 21 hops; -> Boys hostel: 2181m over 22 hops; Dijkstra matched A* on both). | None — `npm test` passes. Phase C continues at Day 9 (wire the Directions button end to end).
- 2026-07-28 | Day 9 | Enabled the PlaceCard Directions button; added src/components/DirectionsPanel.jsx (From dropdown of all places, defaulting to School Gate; To is fixed to whichever place's Directions button was clicked; shows distance and ~1.3 m/s walking time). App.jsx builds the routing graph once from nodes/edges (useMemo) and reruns A* whenever origin/destination change, snapping both to their nearest_node_id. MapView gained a `route` prop that draws/updates a highlighted line via a GeoJSON source+layer. Verified by driving the actual click flow through Chrome DevTools Protocol (marker click -> PlaceCard -> Directions -> panel) against the live dev server: "School Gate -> Admin Building" correctly showed 1.5 km / 20 min walk with the route line rendered along the real surveyed path in a screenshot. | None — try it in the browser: click any marker, then Directions. Phase C finishes at Day 10 (turn-by-turn steps).
- 2026-07-28 | Day 10 | Added src/routing/steps.js: walks the routed node path, computing bearing between consecutive nodes and splitting into "legs" wherever the bearing changes by more than 25°, calling out the nearest place within 50m of each turn point (each landmark used once, never repeating the origin/destination). Wired into DirectionsPanel as a numbered step list below the distance/time summary. Verified against the real campus graph both via a standalone script and via the live browser (CDP): "School Gate -> Library" produced "Turn right after 1.5 km, past Bus Stop. Turn left after 121 m. Turn right after 80 m, past Faculty of Computing. Arrive at Library." — genuinely readable, and a straight route (School Gate -> Admin Building) correctly produced zero turn steps rather than a false one. | None. **Phase C (Routing) is now complete (Days 7-10) — the graded centerpiece works end to end: search or click a place, hit Directions, get a real A*-routed path with distance, walk time, and turn-by-turn steps.** Phase D (location and polish: Days 11-15) is next, starting with live geolocation.
- 2026-07-28 | Day 11 (expanded into full live navigation, at user's request — "look and function like AMAP", not a static one-shot direction list) | Visual redesign: new palette grounded in Federal University Lokoja's actual crest colours (Navy `#0B2545`, Sky `#4FA8E0`) plus a deliberate amber "Confluence" accent (`#F2A93B`) representing the laterite footpaths this app routes over — researched via web search rather than guessed, since this is literally the university's own app. Applied across all existing UI (search bar, category chips, place card, directions panel), not just the new nav surfaces. Built src/location/useLiveLocation.js (watchPosition, with a draggable puck marker in MapView for manual GPS-drift correction — the puck is a compact echo of the crest's concentric shields: navy ring, sky heading cone, amber centre). Built real live navigation: App.jsx recomputes the A* route from the walker's live GPS position on every update (src/routing/graph.js gained nearestNodeId()) — this is inherently self-correcting/rerouting with no separate "off-route" check needed. src/routing/steps.js now returns {text, kind, at} objects (kind drives a direction icon; at is the coordinate used for live distance countdown). New src/components/NavBanner.jsx (top instruction banner, AMAP/Google-Maps convention — next instruction belongs near the eyes, not the thumbs) and NavBottomBar.jsx (remaining distance/ETA + End button). Voice guidance via the Web Speech API (SpeechSynthesisUtterance), speaking each instruction once when it becomes current, with a mute toggle. Arrival detection (within 15m) shows "You've arrived" and auto-exits nav mode after 4s. | **Verification and an important process fix**: discovered mid-session that all of this session's earlier "headless" Chrome testing (Days 7-10) had been silently redirecting into the user's real, already-running Chrome via Windows' default-profile single-instance behaviour — `--headless` was being ignored, and CDP geolocation grants failed because of it. Fixed by launching with an isolated `--user-data-dir`, after which `Emulation.setGeolocationOverride` worked correctly. Re-verified the full flow end to end by simulating a walk (search Library -> Directions -> Start navigation -> stepping the simulated GPS position toward the destination): distance-to-turn counted down accurately at every step (1.8km -> 1.5km -> 1.2km -> 877m -> 602m -> 309m -> 0m), the bottom bar's ETA/remaining distance tracked in lockstep, the instruction correctly simplified as the turn got close, arrival was detected precisely at the destination, and the app auto-returned to normal browsing 4s later. Screenshots confirm the full visual design renders correctly (navy banner, pulsing sky-blue puck on the destination building, monospace ETA in the bottom bar). | **The user should check their own Chrome window for stray tabs** — since testing wasn't properly isolated until partway through this session, some of the automated browser testing for Days 7-10 likely ran in a real, visible tab in their actual browser rather than a hidden headless instance. All testing from Day 11 onward uses an isolated profile and won't touch their real browser. Try live navigation in `npm run dev`: search a place, hit Directions, then "Start navigation" (grant location permission when prompted). Days 12-15 (mobile polish audit, offline support, deploy, field-test) are still open.
- 2026-07-28 | Day 12 | Mobile audit via CDP at a 390x844 (iPhone 12/13-sized) viewport with touch emulation found two real issues: several tap targets under the 44px accessibility guideline (category chips were 35px, search input 41px), and the bottom cards read as floating panels rather than true bottom sheets. Fixed: bumped category chips/search input/close buttons/voice toggle/End button to 44-48px; added a drag handle (src/hooks/useSwipeToDismiss.js) to PlaceCard and DirectionsPanel with a real swipe-down-to-close gesture (touch events, not just a decorative bar); added a scroll-fade hint on the category chip row so its horizontal scrollability is visually obvious. Confirmed zero horizontal overflow at the phone viewport. | None. Verified via CDP: dispatched real synthetic touch events for a 150px downward swipe on the handle and confirmed the card actually closes. MapLibre's own zoom/compass buttons stay small (29px, library-default) — left alone since pinch-to-zoom is the primary touch gesture, not those buttons. Day 13 (offline/service worker) is next.
- 2026-07-29 | Day 13 | Added public/sw.js, a hand-rolled service worker (no framework/plugin dependency): the app shell uses stale-while-revalidate, Supabase REST calls (places/nodes/edges) use network-first with a cache fallback, and the pmtiles basemap is precached whole on install and served to the pmtiles library's HTTP Range requests by manually slicing the cached ArrayBuffer into 206 Partial Content responses (the Cache API doesn't do this on its own). Registered only in production builds (main.jsx checks import.meta.env.PROD) to avoid caching the dev server's ever-changing module URLs. Added LoadingScreen and ErrorBanner (with Retry) components, plus a 15s client-side timeout on the initial data fetch. | **Two real bugs found and fixed via testing, not just written and assumed correct**: (1) a genuinely offline reload with nothing cached showed neither the loading screen nor an error — turned out to be a correct, unavoidable PWA constraint (a device that's never once been online can't load any web app on its first visit), not a bug; but testing a more realistic "Supabase is down while the app shell is already cached" scenario surfaced a real one: (2) a blocked/hanging request left the loading screen spinning forever with no way out — fixed with the 15s timeout — and (3) the error banner visually overlapped the search bar (both absolutely positioned at the same spot) — fixed by making it a normal flex child stacked above the search bar instead. Verified via CDP with a full production build (`npm run preview`) and Chrome's Network domain: cut network entirely after first load -> app still shows the map, all 47 markers, and category filters purely from cache; blocked only the Supabase host -> loading screen correctly gives way to the error banner after 15s, and Retry correctly recovers once unblocked. | None. `npm run build && npm run preview` to try the offline behaviour yourself (DevTools → Application → Service Workers, or Network → Offline). Day 14 (deploy) is next.
- 2026-07-29 | Day 14 | Pushed the repo to github.com/theklvr/ful-pathfinder and deployed via Vercel's git integration (KLVR team). First build failed ("vite: command not found", no install step logged) because the repo's default branch (`main`, auto-created by GitHub with a LICENSE + 1-line README) had never received our actual commit history — everything had been pushed to `master` instead. Merged `master` into `main` (one conflict, on README.md, resolved in favour of the real one) and pushed; the resulting build succeeded. Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Vercel's project env vars (values given to the user directly, safe to expose client-side — Supabase RLS controls actual access). | **Two things the user needs to act on**: (1) the GitHub repo came back `public`, not `private` as originally requested — worth checking github.com/theklvr/ful-pathfinder/settings if that matters. (2) **Vercel's Deployment Protection (SSO) is ON**, so https://ful-pathfinder-klvr.vercel.app currently redirects every visitor to a Vercel login — no student can open it as-is. Turn it off in the KLVR Vercel project's Settings → Deployment Protection. Everything else was verified working on the *live* production URL itself (via a temporary bypass link, not just locally): title, 47 markers, service worker active, search, and a full Directions flow (School Gate -> Library, matching distance/steps from local testing exactly) all confirmed. Local git default branch is now `main` (matches GitHub/Vercel) — commit there going forward; `master` still exists but is no longer the active branch. Real on-campus phone/network testing is still outstanding (Day 15). Day 15 (README, demo script) is next — the "fix issues found in real campus testing" part of Day 15 needs the user to actually go test on campus first.
- 2026-07-29 | Critical fix + AMAP-style visual pass | User reported the deployed app rendering as a completely flat gray screen with only colour dots (no roads, buildings, or basemap at all) and asked for an AMAP/Google-Maps level visual upgrade, sharing real phone screenshots of Google Maps' icons, map-type switcher, and walking-directions UI as reference. **Root cause found (this was a real, deterministic bug, not a "second load" or network flakiness issue as first suspected — an earlier hypothesis about the service worker's hand-rolled pmtiles byte-slicing was ruled out by direct testing and discarded)**: MapLibre GL computes its worker script's URL at runtime relative to its own bundled chunk's `import.meta.url`, using a pattern (`e.endsWith('-dev.mjs') ? ... : 'maplibre-gl-worker.mjs'`) that Vite's static analyzer can't detect — so `vite build` never emitted or copied `maplibre-gl-worker.mjs` into `dist/`. Every production load 404'd fetching it, silently falling back to Vite preview's HTML SPA-fallback response ("Failed to load module script: non-JavaScript MIME type"), the worker never started, and vector tiles never got parsed into renderable geometry — only the plain DOM markers ever painted. **Fix**: copied `maplibre-gl-worker.mjs` and its `maplibre-gl-shared.mjs` dependency from `node_modules/maplibre-gl/dist/` into `public/maplibre/` (committed as static assets, precached by the service worker) and call `setWorkerUrl('/maplibre/maplibre-gl-worker.mjs')` before creating the map. Verified fixed across 4 consecutive fresh loads on a 390x844 mobile viewport, both in Vite preview and with the service worker fully disabled as a control. Also simplified `public/sw.js`'s pmtiles handling from always-slice-a-cached-buffer to network-first-with-cache-fallback (the old approach had an unrelated intermittent race; the real server already serves Range requests natively when online). **Visual pass**: replaced plain colour dots with Google-Maps-style pin markers (CSS teardrop shape, anchor:'bottom') carrying hand-drawn per-category line-icon SVGs (cap for faculty, bed for hostel, briefcase for admin, coffee cup for eatery, banknote for ATM, flag for landmark, wrench for service, medical cross for health, activity line for sport — see `src/data/categoryIcons.js`); made the surveyed path network an actual visible layer (white casing + dashed amber line, up from a near-invisible 0.2-opacity debug line); added a route-line casing for a real-nav-app look; added a light/dark map style toggle button using `@protomaps/basemaps`' named flavors (our free equivalent of Google's map-type switcher — satellite imagery needs a paid provider, ruled out by the free-tools-only constraint). The style toggle initially silently failed to restore the route/path layers after switching (MapLibre's default style-diffing was stalling with custom layers already present and never firing `style.load`); fixed by forcing `{ diff: false }` on `setStyle()`. | None to act on for the fix/visual work — verified and pushed. **Photos are not wired up**: real survey photos exist in `Assets/images/` (27 files, ~104MB, filenames match place names closely) but two things blocked finishing this: (1) no image compression tool was available in this environment without adding a new npm dependency (sharp/imagemagick) — needs the user's go-ahead first per CLAUDE.md's "ask before adding any dependency" rule; (2) the `photo_url` column can't be written — the anon key's RLS policy only allows public *read*, and the Supabase MCP connection needs re-authorization (`claude mcp` or `/mcp` in an interactive session) to do it the way earlier sessions did. Re-authorize Supabase MCP and say go-ahead on a compression tool to unblock this.
- 2026-08-03 | OSM data pipeline (planned with the user, zero new dependencies) | User has personally mapped Felele campus (buildings, footpaths, names) directly on openstreetmap.org via the iD editor, traced against satellite imagery, and saved/uploaded the edits — this is now the ongoing primary source of campus data, merging with (not replacing) the existing 47-place/133-node/139-edge GPS survey. Added `scripts/convert-survey-osm.mjs` (reads a raw Overpass API export — NOT Overpass Turbo's "GeoJSON" export, which discards the OSM node ids that identify shared path junctions — maps OSM tags to the app's 9 fixed categories with a flag-don't-guess fallback, converts footway/path/steps/service/etc. ways into the node/edge shape, with a tight 2m defensive snap for iD editing mistakes) and `scripts/merge-survey.mjs` (folds the OSM-derived staging files into the canonical `data/places.csv`/`data/network.geojson`, detecting duplicate places by name+proximity, merging matching path junctions across the two independently-collected datasets within 12m, and running a connectivity check from School Gate that hard-fails if any place ends up in a disconnected component). Added `data/place-overrides.csv` so manual Supabase edits like `photo_url` survive future re-merges. Smoke-tested both scripts end to end against a synthetic Overpass fixture and the real canonical data (git-restored afterward, no synthetic data left in the repo) — correctly caught a synthetic duplicate "School Gate" node/place, merged one synthetic junction into an existing node, added new nodes/places, and reported a single connected component. `npm test` still passes (routing untouched). No changes to the Supabase schema, `src/routing/*`, `src/map/*`, or the two existing seed scripts. | **User action needed:** run the Overpass query in `docs/SURVEY-GUIDE.md`'s new "Keeping OpenStreetMap in sync" section at overpass-turbo.eu, use **Export → "raw data"**, save as `Assets/felele-osm-export.json`, then run `npm run convert-survey-osm` and `npm run merge-survey`, reviewing each report before running the existing seed scripts. This hasn't been run against the real OSM export yet — only against synthetic test data.
- 2026-08-04 | OSM merge run for real + Phase 1 visual overhaul | User provided the real Overpass raw-data export (`Assets/felele-osm-export.json`, 3088 elements). Ran `convert-survey-osm` then `merge-survey`: network grew from 133 nodes/139 edges to 1883 nodes/1945 edges (~46km of real traced path), places grew from 47 to 48 (19 OSM entries correctly recognized as existing places and skipped, 3 held back as "same spot, different name" for manual review — LT A/Lecture Theatre A, LT B/Lecture Theatre B, Central Mosque/School Mosque). Connectivity check passed (no place in a disconnected component); **not yet seeded into Supabase — `SUPABASE_SERVICE_ROLE_KEY` is missing from `.env`.** Then built the Phase 1 visual pass agreed on with the user (matching Google Maps reference screenshots): persistent name labels next to every map marker (`src/map/MapView.jsx`, `.place-marker-wrap`/`.place-marker-label`); category chips changed from multi-select filter toggles to single-select (`src/components/CategoryFilter.jsx`, `src/App.jsx`) that both filter the map and open a new bottom-sheet list view (`src/components/PlaceList.jsx`) showing every place in that category with a thumbnail/icon, name, description, Directions button, and Share/Call/More actions; a shared `PlaceActions` component (`src/components/PlaceActions.jsx`, `src/utils/share.js`) adds Share (native Web Share API, clipboard fallback) and a More menu (copy coordinates, open in Google Maps) to both the list rows and the place detail card, Call only rendering if a `phone` field is ever present (schema doesn't have one yet); a new "Me" button (`.me-button` in MapView) recenters the map on the browser's live location and shows the heading puck, independent of full turn-by-turn nav mode; the search bar gained a microphone button for voice search via the browser's built-in SpeechRecognition API (renders only if the browser supports it); and a small account icon next to search shows a dismissible "Accounts are coming soon" notice — honest placeholder, not a fake working login, since real accounts are Phase 3. Verified in a real headless-Chromium browser at a 390x844 mobile viewport (Playwright, installed only in the session's scratchpad dir, not added to the project's dependencies): all six pieces worked with zero console errors, confirmed via both screenshots and direct DOM assertions (chip active-state, puck presence, notice visibility). `npm run lint` and `npm test` both pass. | **Two things needed:** (1) add `SUPABASE_SERVICE_ROLE_KEY` to `.env` (from Supabase dashboard → Settings → API) so the merged OSM data can actually be seeded into the live database — ask and I'll run the seed scripts; (2) look at the app in a real browser (`npm run dev`) to react to the new visual pass before we move on to Phase 2.
- 2026-08-04 | Fixed two real gaps user flagged after looking at the Phase 1 pass | (1) **Building shapes were invisible**: `public/map/felele.pmtiles` is a frozen basemap extract from before the campus was mapped in OSM, and Protomaps' builds lag live OSM by days, so the traced buildings would've stayed invisible regardless of how much gets mapped. Fixed by drawing them ourselves: `scripts/convert-survey-osm.mjs` now also extracts every building way's full polygon ring (not just its centroid) from the same Overpass export into `public/map/campus-buildings.geojson` (152 footprints) as a separate visual-only output — doesn't touch the places/network pipeline or Supabase. `src/map/MapView.jsx` fetches and renders it as a fill+outline layer, inserted below the path-network layer. (2) **Place card/list "modal" didn't match the Google Maps reference images**: replaced the old full-width text button + separate pill row with a single icon-over-label action row (Directions/Call/Share/More, `src/components/PlaceActions.jsx`), matching Google Maps' actual card convention — used identically in both the place detail card and the category list rows now. Verified in a real headless browser: building polygons render correctly under the markers, action row renders and the More menu still opens, zero console errors, lint/tests still pass. | Look at it again in a real browser and react — this was two rounds of "doesn't match the reference" feedback, want to catch anything still off before continuing to Phase 2.
- 2026-08-04 | Phase 2: free-pick Directions | Added a blue Directions FAB on the map (`.directions-fab` in `src/map/MapView.jsx`) that opens the directions panel with nothing preselected — previously the only way in was tapping a specific place's Directions button. Both "From" and "To" are now typeahead fields (new `src/components/PlaceSelectField.jsx`, same name/alias matching as the search bar) instead of a plain dropdown with a fixed destination, so either side can be freely searched and changed. Added a reverse/swap button between them (disabled until both are set) that swaps origin and destination in one tap. `src/App.jsx` gained a `showDirections` flag independent of `destinationPlace` so the panel can open empty; `DirectionsPanel` now handles all three states cleanly (no destination yet / no origin yet / route found / no route exists) instead of assuming a destination is always present. Verified in a real headless browser: opened directions with nothing picked, typed "Faculty of Law" into To, selected it, got a real routed distance/time/steps, hit reverse and confirmed From/To actually swapped (School Gate → Faculty of Law became Faculty of Law → School Gate) — zero console errors, lint/tests pass. | Try it in `npm run dev`: tap the new blue directions button on the map (not a place first) and pick both ends yourself.
- 2026-08-04 | Phase 3: real accounts | Email/password auth needed no dashboard toggle — Supabase turns it on by default for every project. Added `supabase/migrations/20260804090000_accounts_favorites_reviews.sql` (new `favorites` and `reviews` tables linked to `auth.users`, RLS: reviews public-read/owner-write, favorites fully private to the owner) — **not yet applied to the live database**, no Supabase MCP connection this session to run it directly. Added `src/lib/auth.js` (`useAuth()` hook wrapping `supabase.auth`) and `src/components/AccountPanel.jsx` (sign in/up tabs, real form, error/confirmation messaging) — replaces the old "Accounts are coming soon" placeholder from Phase 1 with the real thing. Wired into `src/App.jsx`/the account button in `src/index.css`. Verified the UI itself in a real browser (tab switching, form rendering, no console errors) but **did not submit an actual sign-up** — that would create a real row in the live Supabase auth users table, and this session has no way to clean that up afterward, so it needs a real decision from the user rather than a test account I create silently. | **User action needed:** open the Supabase dashboard → SQL Editor, paste in `supabase/migrations/20260804090000_accounts_favorites_reviews.sql`, and run it — that's what actually creates the `favorites`/`reviews` tables live. After that, try signing up for a real account in the app yourself to confirm the end-to-end flow (favorites/reviews UI itself isn't built yet, this pass is just making accounts real).
- 2026-08-04 | Phase 3 continued: favorites + reviews UI | User confirmed the migration was applied. Built the two features the new tables were for: (1) a Save/bookmark button on the place card (`src/components/PlaceCard.jsx`, `src/data/favorites.js`) that fills amber when saved, prompts sign-in if you're not logged in rather than silently failing; (2) real star ratings and reviews (`src/components/PlaceReviews.jsx`, `src/components/Stars.jsx`, `src/data/reviews.js`) — a 5-star picker plus optional comment, upserts so re-rating a place you already rated updates it instead of erroring on the table's unique constraint, shows "No ratings yet" honestly when a place has none rather than faking a number, and the category list view now shows each place's real average rating (one bulk query per category, not one query per place). Verified in a real browser signed OUT (to avoid writing a test account into the live database): rating/favorite data loads without errors (reviews are public-read per the RLS policy, favorites correctly returns empty for no session), clicking Save or a star while logged out correctly opens the sign-in panel instead of failing silently. Lint/tests pass. | **Still not tested signed IN** — same reasoning as last entry, submitting a real sign-up from this session would leave a permanent account in the live database with no way to clean it up. Sign up for real, then try saving a place and leaving a review yourself to confirm the write path actually works end to end.
- 2026-08-04 | Phase 4: crowdsourced place submissions | Added `supabase/migrations/20260804120000_place_submissions.sql` (`place_submissions` table -- users can insert/read their own rows only, RLS blocks seeing anyone else's; moderation and promotion require the service role, outside normal user access -- **not yet applied, needs the same SQL Editor paste as the last two migrations**). New "Add a place" flow: a `+` button on the map (`src/map/MapView.jsx`) arms pin-drop mode (crosshair cursor, a cancel-able hint banner), the next map tap opens `src/components/SubmitPlaceForm.jsx` pre-filled with those coordinates (name, category, nicknames, description, photo URL, submits into `place_submissions` via `src/data/submissions.js`). AccountPanel now lists the signed-in user's own submissions with a pending/approved/rejected status pill. Moderation stays deliberately low-tech for now, consistent with the OSM pipeline's philosophy: a human reviews pending rows directly in the Supabase table editor and flips `status`; `supabase/seed/promote_submissions.mjs` (new `npm run promote-submissions`) then copies approved-and-not-yet-promoted rows into the real `places` table using the service role key, the same pattern as the existing seed scripts. **Caught and fixed a real bug during verification**: the new `+` button had no CSS positioning at all (missed adding it to the shared floating-button rule) -- it silently rendered off in normal document flow, invisible-but-present, with the map canvas capturing all its clicks; a headless-browser click test hung against it, which is what surfaced it. Fixed and re-verified: the full button stack (Me/Directions/Add place/style toggle) is now correctly positioned and clickable, and clicking "+" while signed out correctly opens sign-in instead of arming add-place mode. Lint/tests pass. | **User action needed:** paste `supabase/migrations/20260804120000_place_submissions.sql` into the Supabase SQL Editor and run it. As with favorites/reviews, the actual signed-in submit-a-place flow hasn't been tested end to end from this session (would write a real row) -- once the migration's applied, try adding a real place yourself, then either flip its status to 'approved' in the table editor and run `npm run promote-submissions`, or tell me once `SUPABASE_SERVICE_ROLE_KEY` is in `.env` and I can run it. Deferred from this pass, flagged not forgotten: edit-suggestions for *existing* places (only new-place submissions are built), real file upload for photos (URL field only, since creating a Storage bucket needs the same missing service-role access), and rate-limiting/abuse-prevention on the submission form.
- 2026-08-04 | Satellite map view (Esri World Imagery) | Added a real satellite basemap option: `src/map/style.js` gained a 'satellite' style (Esri World Imagery XYZ raster tiles, free/no API key, same basis as the existing OSM/Protomaps basemap). The old light/dark-only toggle became a proper "Map type" popover (`src/map/MapView.jsx`) with Default/Dark/Satellite options, matching the Google Maps reference. Building outlines dim to a faint white line over satellite instead of the solid tan fill used elsewhere, so the actual aerial photography stays visible underneath. **This took three real, compounding bugs to actually get working, all found and fixed during verification, not shipped blind:** (1) the effect that applies a style change only re-ran when `flavor` itself changed, not when the map's first load finished -- so tapping the map-type button in roughly the first second after page load got silently dropped and never retried; (2) fixing that by adding `mapLoaded` as a trigger introduced a new bug -- it caused a pointless, redundant `setStyle` call the instant the map finished loading even when flavor hadn't changed (the map already had that exact style from construction), and that redundant reload is what was actually racing with the async campus-buildings fetch, throwing "Style is not done loading" and crashing the whole app via the error boundary; (3) fixed properly by comparing against the flavor the map was actually constructed with (`initialFlavorRef`), only reapplying if the user changed flavor before the map's first load completed, plus a defensive try/catch-and-retry-on-idle around the buildings-layer addSource call as a second line of defense since isStyleLoaded() alone wasn't fully reliable. Verified by cycling light -> dark -> satellite -> light -> dark repeatedly in a real browser: zero crashes, zero console errors, every source (esri-satellite/protomaps, network-debug, route, campus-buildings) present and correct at every step, real aerial imagery visibly rendering with tile requests confirmed returning 200. Also found and fixed an unrelated environment problem while debugging this: the "stop the dev server" commands used all session had been silent no-ops because `lsof` doesn't exist on Windows/Git-Bash -- 13 stray dev-server processes and 17+ orphaned Chrome processes from crashed test runs had piled up, contributing to the instability. Cleaned up and switched to a Windows-native `Get-NetTCPConnection`/`Stop-Process` approach. Lint/tests pass. | None -- this is complete and verified, including a regression sweep across every map-type combination.
- 2026-08-09 | Directions/navigation bug fixes (user-reported: couldn't route from live location, camera fought manual panning, card covered the map on PC) | Root causes found by reading the actual code, not guessed: (1) `PlaceSelectField`/`DirectionsPanel` only ever offered surveyed places as the "From" origin, never live GPS -- added a "Your location" sentinel (`src/data/myLocation.js`) selectable in the From field, with `App.jsx`'s `staticRoute`/`staticSteps` resolving it against live `userPosition` via `nearestNodeId`, plus locating/permission-denied messaging. (2) `MapView.jsx`'s nav camera-follow effect unconditionally `easeTo`'d to the walker's position on every GPS tick with no way to pause it -- added a `dragstart` listener that pauses following, and a recenter button that reappears until tapped. (3) `.place-card`/`.place-list` had zero desktop breakpoint (mobile bottom sheet up to 70vh tall, full width) -- added a `min-width: 720px` media query making them a 380px left-anchored side panel instead, like Google Maps' own desktop layout. **Testing (real headless Chromium, not just read-and-assume) also caught an unrelated pre-existing bug**: the category-chip row had no z-index and was silently intercepting clicks meant for the search-results dropdown underneath it -- fixed by giving `.search-bar` a higher z-index. Verified: "Your location -> Library" routes correctly (1.2km/16min); the pause/recenter state logic fires correctly (confirmed by directly firing MapLibre's `dragstart` event, since headless CDP can't reliably fake a real drag gesture for map libraries -- real-device confirmation still needed for the gesture itself); desktop card now takes 30% width instead of covering most of the map. `npm run lint` and `npm test` pass. | **Please confirm on your actual phone**: does dragging the map during live navigation now stay put (with a recenter button appearing) instead of snapping back? That one piece couldn't be verified by automation.
- 2026-08-09 | Quick UI-polish batch (marker size, button placement, collapsible chips, search recents, scale bar, zoom decluttering) | Six small, independently-verified changes: (1) shrunk place-marker pins 30px -> 22px. (2) Moved the My Location/Directions/Add Place/Map Style buttons from the top-right corner to a bottom-right stack (thumb-reachable), and hid them during navigation since `NavBottomBar`/the recenter button own that corner then. (3) `CategoryFilter` gained a collapse toggle (chevron button) so the chip row can be hidden. (4) Search now shows a "Recent" list (`src/data/recentSearches.js`, localStorage-backed, capped at 8, re-resolved against live place data so renamed/removed places never show stale) when the search box is focused with no query typed yet, with a Clear button. (5) Added MapLibre's built-in `ScaleControl` (metric + imperial, bottom-left) so distance-to-pixel scale updates live as you zoom. (6) Marker visibility now declutters by zoom: major categories (faculty/admin/hostel/landmark) always show, smaller amenities (eatery/atm/service/health/sport) only appear from zoom 15.5-16.5+ -- this is a category-based heuristic, not real surveyed "importance" data (no such field exists), and is skipped entirely when a category filter is actively selected so it never fights explicit user intent. Verified all six in a real headless browser: marker AABB shrunk as expected, all four buttons measured at their new bottom offsets (12/64/116/168px), chip row toggles visible/hidden, searching then reopening search showed the place under "Recent", two scale controls present, and marker count went 34 (zoom 14) -> 47 (zoom 18). `npm run lint` and `npm test` pass. | None -- try it in `npm run dev`. Still open from the original request: account system overhaul (Google login, profile, lists/favorites/visited/timeline, home/work), Explore/Contribute bottom-nav tabs with a contributor points/badge system, per-place image pipeline, and (flagged earlier as not free-tools-compatible) street view and a traffic layer.
- 2026-08-09 | Weather + air quality | Added `src/data/weather.js` (fetches Open-Meteo's forecast + air-quality APIs in parallel -- free, no API key, no billing, so no new-dependency or paid-service conversation needed) and `src/components/WeatherWidget.jsx`: a compact "icon + temperature" pill next to the account button (`src/App.jsx`'s search-row) that opens a small panel on tap showing condition, feels-like, humidity, wind, and a US-EPA-style AQI category (Good/Moderate/Unhealthy.../Hazardous) with PM2.5. WMO weather codes are mapped to a condensed icon set matching Lokoja's actual tropical climate (sun/cloud/fog/rain/storm) rather than every code the spec defines. Verified against live data in a real browser: correctly showed Lokoja's actual current conditions (25°C, Overcast, feels like 29°C, 91% humidity, 12 km/h wind, AQI 58 Moderate). `npm run lint` and `npm test` pass. | None -- try it in `npm run dev`. Still open: account overhaul, Explore/Contribute tabs + contributor system, image pipeline, street view/traffic (flagged as not free-tools-compatible).
- 2026-08-09 | Account overhaul, step 1: profile (name + photo) | Scoped the "account overhaul" ask with the user first -- it bundled 7+ distinct features (Google login, profile, switch account, location sharing, add-your-business, personalization, settings), two of which needed real decisions before writing code (Google login needs an external Google Cloud OAuth client only the user can set up; "location sharing" turned out to mean full friends/contacts live sharing, a privacy-sensitive feature getting its own dedicated pass later, not folded in here). Agreed to do the rest step by step. This step: added `supabase/migrations/20260809140000_profiles.sql` (a `profiles` table -- public-read so a name can show next to reviews/submissions later, owner-only write -- plus an `on_auth_user_created` trigger that auto-seeds a profile from whatever the auth provider already gave us, so Google's future name/photo lands here with zero extra code, and email/password signups fall back to the email's local part). Added `src/data/profiles.js`, `src/lib/useProfile.js` (shared between the account button and panel so a save in one updates the other without a second fetch), and extended `AccountPanel.jsx` with an avatar + display name shown at the top and an "Edit profile" form (name + photo URL -- URL only, not a real upload picker, since that needs a Storage bucket, the same blocker flagged for place photos back on 2026-08-04). The account button in the top bar now shows the real avatar image once one's set, instead of the generic person icon. Verified in a real browser signed OUT (writing a real profile row would mean a permanent row in the live database with no way to clean it up from this environment, same constraint as every other account-related pass): panel opens, tabs render, zero console errors. `npm run lint` and `npm test` pass. | **Two things needed:** (1) paste `supabase/migrations/20260809140000_profiles.sql` into the Supabase SQL Editor and run it -- this migration wasn't applied automatically. (2) Once applied, sign in for real and set your own name/photo to confirm the write path -- same reasoning as every prior account feature, this session can't test the signed-in path without leaving a permanent account behind. Next step in the account overhaul (still your call on order): Google login, "add your business" entry point, or settings/personalization.
- 2026-08-09 | Account overhaul, steps 2-4: add-your-business entry point, settings/personalization, Google login | (2) Added an "Add your business" button inside the signed-in `AccountPanel` (`src/components/AccountPanel.jsx`) that closes the panel and arms the existing crowdsourced add-place flow (`onStartAddPlace`, built back in Phase 4) -- no new backend, just a clearly-labelled entry point, as scoped with the user. (3) Settings/personalization: new `profiles`-style local pattern but device-local, not account-synced -- `src/data/settings.js` (localStorage) + `src/lib/useSettings.js` + `src/components/SettingsPanel.jsx`, reachable via a gear icon in `AccountPanel` from *either* the signed-in or signed-out view (settings aren't account data). Covers distance units (metric/imperial), default map style (light/dark/satellite, read by `MapView`'s `initialFlavor` prop), and a voice-guidance-during-nav default. Threading the unit choice required touching `src/routing/format.js` (`formatDistance` now takes a `unit` param; imperial shows ft under 0.1mi, mi above) and `src/routing/steps.js` (dropped its own duplicate distance formatter in favor of importing the shared one, now unit-aware) plus every place a distance renders: `DirectionsPanel`, `NavBanner`, `NavBottomBar`. (4) Google login: `src/lib/auth.js` gained `signInWithGoogle()` (`supabase.auth.signInWithOAuth({ provider: 'google' })`), wired into a real "Continue with Google" button in `AccountPanel`. **This is code-complete but not usable yet** -- Google OAuth needs a Google Cloud OAuth client (external, Google Cloud Console) wired into Supabase's Auth provider settings, which only the user can do; I cannot create Google Cloud resources. Verified everything in a real browser: settings panel opens from both signed-in and signed-out states, switching to imperial immediately changed a live directions result from "1.2 km" to "1.2 mi", the choice persisted to localStorage, and the Google button correctly redirects to Supabase's own `/auth/v1/authorize?provider=google` endpoint (confirming the client-side wiring is right) with zero console/page errors -- it'll show Supabase's own "provider not configured" response until the setup below is done. `npm run lint` and `npm test` pass. | **To finish Google login (the user must do this, it's an external console)**: (1) In Google Cloud Console, create a project (or reuse one), go to APIs & Services -> Credentials -> Create OAuth client ID -> Web application. (2) Add authorized redirect URI: `https://qvwgeowgidwzyaztyhdu.supabase.co/auth/v1/callback`. (3) Copy the generated Client ID and Client Secret. (4) In the Supabase dashboard -> Authentication -> Providers -> Google, paste them in and enable the provider. (5) Try "Continue with Google" in the app -- should complete a real sign-in and, via the `handle_new_user` trigger from the profiles migration, auto-populate your display name and photo from your Google account. Also still needed from step 1: run `20260809140000_profiles.sql` if not done yet. Remaining from the original account-overhaul ask: friends/contacts live location sharing -- deferred on purpose, gets its own dedicated scoping pass given the privacy stakes.
- 2026-08-09 | Explore / You / Contribute tabs + contributor points/badges | Added `supabase/migrations/20260809160000_explore_you_contribute.sql` (**not yet applied -- paste into the Supabase SQL Editor**): `place_edit_suggestions` (covers "update place", "add photo", "update address", and "report a road problem" all through one reviewed-by-a-human table, same low-tech moderation pattern as `place_submissions` -- deliberately does NOT allow live edits to `places` or the routing graph, since letting untrusted input mutate the surveyed path network directly would risk corrupting routing), `lists`/`list_items` (user-created named lists beyond the existing single Favorites table -- "Want to go", "Saved trips", anything custom), `visited_places` (written once per arrival during live navigation, doubles as the Timeline view grouped by date client-side), and `home_lat/home_lng/home_label/work_lat/work_lng/work_label` columns on `profiles`. New data layer: `src/data/editSuggestions.js`, `src/data/lists.js`, `src/data/visited.js`, `src/data/contributorStats.js` (points/level/badge computed live from real counts in existing tables -- 10pts per approved place, 5 per approved edit, 2 per review, level = points/25, badges Newcomer->Campus Legend -- not invented numbers, just aggregated ones), plus `updateHomeWork` added to `src/data/profiles.js`. New UI: `src/components/BottomNav.jsx` (Explore/You/Contribute tabs, hidden during live navigation same as the other FABs), `src/components/YouPanel.jsx` (recent places via the existing recents store, Favorites + custom lists with create/delete, home/work set via the existing place-search typeahead, timeline grouped by day), `src/components/ContributePanel.jsx` (contributor stats card, Add a place/Add a review/the four edit-suggestion kinds, all gated behind sign-in), `src/components/AddToListButton.jsx` (small popover on `PlaceCard`, next to the existing favorite-star button, to save a place into any list or create one inline). Selecting a place from any tab (recents, lists, timeline, "add a review") routes through the existing `handleSelectPlace`, which now also switches back to the Explore tab so the map/card is what's visible. Threading the new bottom nav in required bumping every other bottom-anchored element (the FAB stack, place-card/place-list/directions-panel bottom sheets, both mobile and the desktop side-panel breakpoint) by a `--bottom-nav-height` CSS variable so nothing sits underneath it. **Two real bugs caught and fixed during verification, not shipped blind**: (1) `ContributePanel`'s "Add a review" flow was calling a parent state-setter during render (a React anti-pattern that can produce a "Cannot update a component while rendering a different component" warning) -- moved into a `useEffect`; also the 'review' kind isn't in the `EDIT_KINDS` list used for the generic suggestion form, so without a dedicated branch it would have crashed on `kindInfo.label` the first time someone tapped "Add a review" -- added a distinct render path for it. (2) the new `.tab-panel` bottom-sheet's mobile `max-height` was still winning at desktop width, leaving an empty 132px gap under the side panel instead of filling it -- root cause was CSS cascade source order (a later same-specificity rule beats an earlier one regardless of the media query), not a specificity bug; fixed by adding the desktop override after the base rule instead of before it. Verified in a real headless browser at both a 390x844 mobile viewport and a 1280x800 desktop viewport: bottom nav renders and switches tabs correctly, You/Contribute correctly prompt sign-in when signed out (didn't test the signed-in write paths for the same reason as every other account feature this session -- a real submission/list/visit would be a permanent row in the live database), the FAB stack measured 12px clear of the new nav bar, Explore tab's search-and-select flow still works unchanged, the add-to-list button opens and correctly gates on sign-in, and the desktop side panel now correctly fills to within 12px of the nav bar. `npm run lint` and `npm test` pass. | **Needed:** paste `supabase/migrations/20260809160000_explore_you_contribute.sql` into the Supabase SQL Editor and run it (needs the profiles migration from earlier today applied first, since it adds columns to that table). Then, signed in for real: try creating a list and saving a place to it, submitting each of the four edit-suggestion kinds, adding a review via the Contribute tab, setting home/work, and doing a real navigation to completion to confirm a Timeline entry appears -- none of that was exercised signed-in from this session. Two things deliberately deferred, not forgotten: moderation/promotion tooling for `place_edit_suggestions` (a human can already review rows directly in the Supabase table editor, same as submissions, but there's no `promote-submissions`-style script yet to actually apply an approved edit to the live `places` row -- that's a follow-up), and friends/contacts live location sharing (still wants its own scoping pass).
- 2026-08-09 | Grayscale map style, add-a-place from live location, draggable variable-height sheets, Profile/Sign-in as real pages | (1) Added Grayscale as a third free Protomaps flavor (`@protomaps/basemaps` ships DARK/WHITE/GRAYSCALE/BLACK/LIGHT -- only light/dark were exposed before) to both the map-type menu and Settings' default-style picker -- a real "other kind of map" at zero cost, unlike street view/traffic/a cycling layer which all still have no free equivalent (reconfirmed, not re-litigated -- see prior entries). (2) Add-a-place now offers "Use my location" in the pin-drop hint banner when GPS is available, submitting the walker's live position directly instead of requiring a map tap first (`src/App.jsx` extended the existing `useLiveLocation` enable condition to include `addPlaceMode`, `MapView.jsx` gained `onUseMyLocationForNewPlace`). (3) **Rewrote `src/hooks/useSwipeToDismiss.js`** (same exported name/shape, zero call-site changes needed in `PlaceCard`/`DirectionsPanel`/`SubmitPlaceForm`) from a fixed-height swipe-to-dismiss into a real 3-snap-point draggable sheet (peek/half/full, matching the Google Maps reference the user shared) -- drag the handle up to expand, down to shrink, or far enough down to dismiss; also added the same handle to `PlaceList` (previously had no drag affordance at all, close-button only). Required loosening the mobile `max-height` caps on `.place-card`/`.place-list`/`.directions-panel` (they were capping at 60-70vh, which would have silently clamped the new "full" snap below its target) and making `.place-card`/`.place-card-body` genuinely flex/scroll-managed on mobile, not just at the desktop breakpoint. (4) **Profile and Sign-in are now real full-screen pages** (`position: fixed; inset: 0`), not the small dropdown card they were before -- `AccountPanel.jsx` restructured around a shared `PageHeader` (title + settings/close icon buttons), Settings absorbed the same treatment. Removed the resulting dead CSS (`.account-panel` and `.account-settings-button`'s old popover-card rules, `.settings-panel`'s own card chrome and header -- both fully superseded, confirmed unused via grep before deleting, not left as cruft). **Testing caught the max-height clamp issue on the draggable sheet** (verified via real CDP touch-drag events, not just code review): dragging to "full" measured 684px against a 760px JS target, traced to the loosened-but-still-present `calc(90vh - ...)` ceiling -- decided this was acceptable (still 81% of the screen, matches the ask, not worth a further chase) rather than silently shipping a mismatch unexplained. Verified in a real browser: all three snap points measured correctly (peek 287px, half 523px, full clamped-684px) via simulated touch drags, drag-past-threshold dismiss works, desktop's fixed side-panel layout confirmed completely unaffected (no inline height, same box as before), grayscale switches with zero console errors, and the sign-in page now genuinely covers the full viewport with Settings reachable from within it. `npm run lint` and `npm test` pass. | None to act on for what shipped here -- try it in `npm run dev`, especially dragging a place card open on your phone. **Two things need your input, not code**: (1) weather/air quality and the "paste a URL and a photo shows" mechanism the user asked about **already exist** (built 2026-08-09 and across Phase 4) -- flagging in case they weren't discovered rather than actually broken; the real remaining photo gap is the 27 real survey photos in `Assets/images/` still not wired to the live `places` rows (blocked since 2026-08-04 on a compression-tool go-ahead and a working `SUPABASE_SERVICE_ROLE_KEY` -- say go-ahead and I'll unblock it). (2) The user asked for the Google OAuth redirect to work on `ful-pathfinder.vercel.app`, but `CLAUDE.md` documents the live URL as `ful-pathfinder-klvr.vercel.app` -- these don't match. The code itself doesn't need a fix (`signInWithGoogle` already uses `redirectTo: window.location.origin`, which works for whichever domain actually serves the app); what's actually needed is adding the real production URL to Supabase's Authentication -> URL Configuration -> Redirect URLs allow-list. **Confirm the actual current Vercel domain** so this gets configured against the right one, not guessed.
- 2026-08-10 | Photo pipeline unblocked, scale-bar fix, Vercel domain corrected | User confirmed the live domain is `ful-pathfinder.vercel.app` (not `-klvr`) and gave go-ahead to unblock the survey-photo pipeline. **Photos**: added `sharp` as a devDependency (free, MIT, the standard Node image library -- no paid API) and `scripts/prepare-photos.mjs`, which resizes each of the 28 real survey photos in `Assets/images/` (currently ~105MB, some over 5MB each -- exactly the "lag on a weak campus network" problem) to max-1600px-wide WebP at quality 78, uploads them to a new `place-photos` Supabase Storage bucket (`supabase/migrations/20260810090000_place_photos_bucket.sql`, not yet applied), and conservatively matches filenames to real place names. Dry-ran the compression against real files before asking the user to trust it: 3.3MB->223KB, 2.5MB->136KB, 5.3MB->169KB (93-97% smaller). **Caught a real matching bug via dry-run testing, not shipped blind**: the first matching approach (trust whichever word matched the fewest places) silently assigned a mosque photo to an unrelated lecture theatre, because both "mosque" and "memorial" happened to each uniquely identify a *different* place and the tie broke arbitrarily on word order. Fixed by treating that exact situation -- two different words each uniquely pointing to two different places -- as a conflict to skip, not a coincidence to resolve; only auto-applies when all unique-match words agree on the same place. Re-ran the corrected logic against the real 28 filenames and 48 place names: 16 confident auto-matches, 12 correctly left for manual review (including the mosque/theatre case, now flagged as a conflict instead of silently wrong), zero forced/guessed assignments. Also added `npm run prepare-photos`. (2) Fixed the map's scale/distance indicator (added 2026-08-09) being invisible -- it was rendering directly underneath the new opaque bottom nav bar, at the same bottom-left corner, same problem the FAB stack and card sheets already had and were fixed for, just missed for this one control since it's MapLibre's own built-in element rather than ours. Used `!important` on `.maplibregl-ctrl-bottom-left` since maplibre-gl.css's load order relative to our own stylesheet isn't guaranteed by the bundler. Verified: scale control's bottom edge now sits exactly flush with the nav bar's top edge (780px = 780px), fully visible. (3) Corrected `CLAUDE.md`'s documented production URL. `npm run lint` and `npm test` pass. | **User action needed**: (1) paste `supabase/migrations/20260810090000_place_photos_bucket.sql` into the Supabase SQL Editor. (2) Add `SUPABASE_SERVICE_ROLE_KEY` to `.env` (Supabase dashboard -> Settings -> API -> service_role) -- this script can't run without it, same requirement as every other seed/promotion script in this repo. (3) Run `npm run prepare-photos`, then look at the printed report and manually assign `photo_url` (via the Supabase table editor, or an edit-suggestion) for the 12 filenames it couldn't confidently match by itself.
- 2026-08-10 | Public routing/places API for other student groups (chatbot, exam-venue platform) | Scoped with the user first: read-only place data + walking routes, fully public (no API key, same anon-key read posture the app itself already uses), MCP explicitly deferred until this REST layer is actually in use. Places data needed no new code -- Supabase already auto-generates a public REST API for every table (PostgREST), so `docs/API.md` documents how to call it directly (`GET /rest/v1/places?...`) rather than wrapping something that already works. Routing did need new code, since A* only ever ran client-side in the browser: added `api/route.js`, a Vercel serverless function (auto-detected zero-config from the `/api` directory, no `vercel.json` needed) that imports the existing `src/routing/{graph,astar,haversine,steps,format}.js` directly -- confirmed these have zero browser-only dependencies, so the exact same routing logic runs server-side with no duplication. Accepts `from`/`to` as either a known place name/alias or a raw `"lat,lng"` pair (so an exam-venue platform can route from a student's live GPS position to a venue's coordinates, not just between named places), with the same "don't guess on ambiguity" principle as `scripts/prepare-photos.mjs` -- an ambiguous or unmatched name returns a 404 with a clear message rather than silently picking one. CORS set to `*` since this is meant to be called from other teams' own domains. Verified by invoking the handler directly against the real (read-only) database with a mock req/res, since `/api` serverless functions don't run under the plain Vite dev server: School Gate -> Library returned distance/time/turn-by-turn text identical to the app's own known-good result from the 2026-07-28 Day 10 entry, error cases (missing param -> 400, unknown place -> 404) behaved correctly, and a raw `"lat,lng"` origin resolved and routed correctly. `npm run build` still succeeds with the new `/api` directory present (Vite correctly ignores it). `npm run lint` and `npm test` pass. | None to act on -- this deploys automatically with the next push to `main`, same as everything else. Once deployed, `https://ful-pathfinder.vercel.app/api/route?from=School+Gate&to=Library` should work directly; share `docs/API.md` with the other groups. MCP server is intentionally not built yet, per the user's own sequencing (API first, MCP once it's proven out).
- 2026-08-10 | Photo script fixes, real GPS-permission bug fix, logo branding, dev/API settings page | User ran `npm run prepare-photos` for real: 15/28 uploaded (105MB -> 2.2MB for those), 13 failed with transient "fetch failed" errors in one contiguous alphabetical batch then recovered on their own -- a network blip, not a code bug, but exposed two real gaps: (1) no retry, so a blip mid-run meant re-running the whole script by hand -- added a 3-attempt retry with backoff. (2) two matching misses: `ful_logo.png` (the KLVR/FUL brand logo, not a campus photo) got pulled into the pipeline and correctly flagged "no match" but shouldn't have been considered at all -- added it to an exclusion list; and "Centre for ICT.jpg.jpeg" failed to match the real "ICT" place because the word-length filter required >=4 characters, silently dropping the one 3-letter word that would've matched -- lowered the floor to 3 (safe because matching is whole-word-boundary regex already, so a short word can't false-positive inside a longer one) plus a small stopword list. Re-verified the matching logic against the real 28 filenames and 48 places after the fix: ICT now matches correctly, the logo is excluded, the earlier mosque/lecture-theatre conflict still correctly stays a conflict (not a regression). **Real bug, not a script issue**: "always asking for my location even when not needed" -- traced to `src/App.jsx`: `originPlace` (the Directions "From" field) is sticky for the whole session once set, so picking "Your location" as an origin even once left the live GPS watch running in the background indefinitely, including after Directions was closed and nothing was displaying the position anymore. Fixed by gating live GPS on `showDirections && originIsLiveLocation` instead of `originIsLiveLocation` alone. Verified precisely, not just by reading the diff: instrumented `navigator.geolocation.watchPosition`/`clearWatch` in a real browser and counted calls -- picking "Your location" starts the watch (1 call), closing Directions stops it (`clearWatch` fires), reopening Directions with that origin still selected correctly restarts it. **Branding**: generated `public/favicon.png` (64x64) and `public/logo.png` (480px) from `Assets/images/ful_logo.png` via `sharp` (815KB source -> 3.8KB / 77KB, same "don't ship huge images" reasoning as the photo pipeline), wired into the browser tab icon, the loading screen, and the sign-in page; removed the now-unused default `public/favicon.svg`. Removed the horizontal-scroll fade-gradient hint on the category chip row per the user's "remove that scroll arrow thing" ask -- the only such affordance in the codebase. **Developer/API section**: added `src/components/DeveloperPanel.jsx`, reachable from Settings, showing the Supabase REST base URL + anon key (copy buttons) for places data and the `/api/route` endpoint with live examples, built from the same content as `docs/API.md` -- the anon key shown here isn't a new exposure, it's already shipped in the app's own browser bundle. Verified in a real browser: logo/favicon load with 200s, the scroll-hint pseudo-element is gone, the sign-in page shows the logo, the developer panel is reachable and mentions both endpoints correctly, zero console errors. `npm run lint` and `npm test` pass. | **Re-run `npm run prepare-photos`** to pick up the 13 that failed to upload plus the "ICT" fix -- safe to re-run, already-matched places are skipped, already-uploaded files just get re-uploaded to the same path. Two photos still need your own eyes regardless of the script fixes (genuinely unresolvable from a filename alone): the ALHAJI ABDUHLLAHI HABIB MEMORIAL MOSQUE photo is almost certainly `School Mosque` (that's my read of it, but you know the campus -- confirm and set `photo_url` yourself, since the script correctly refuses to guess between it and the lecture theatre both containing "memorial"). Still open from the original message: navigation "runs long / wrong direction" (need to know if this was tested on-campus or off -- off-campus, the route would look wrong simply because the surveyed network is Felele-campus-only, not a bug), location sharing via link, and the admin moderation panel -- next up.
- 2026-08-10 | Nav bug confirmed expected (off-campus testing), location sharing via link built | User confirmed the "runs long / wrong direction" nav report was from testing off-campus -- expected given the Felele-only survey network, not a bug, no fix needed. **Location sharing**: new `location_shares` table (`supabase/migrations/20260810120000_location_shares.sql`, not yet applied) -- a UUID primary key doubles as the unguessable share token (same model Google Maps' own sharing links use), public-read only while `active=true and expires_at > now()` so a link stops working on its own after 4 hours by default rather than sharing indefinitely, owner-only write. `src/lib/useLocationSharing.js` lives at the App level, not inside a tab component -- a share tied to a mounted component would silently stop updating the moment the user switched away from the You tab, which isn't what "share my location" should mean; a two-step start (arm GPS via a `wantsToShare` flag, then create the row once a real fix arrives) exists because there's no position to share until GPS -- which that same flag turns on -- has produced one. Position writes are throttled to once per 15s client-side, not every raw GPS tick. Sharer-side UI added to `YouPanel` (start/stop, copyable/shareable link, reuses the existing Web Share API helper -- extended `src/utils/share.js` with a generic `shareUrl()` for it). Recipient side: `src/components/SharePage.jsx`, a genuinely separate lightweight page (not a view inside the main app) since the recipient may have no account and no reason to load the rest of the app -- `main.jsx` gained a plain pathname check for `/share/:id` (no router library added, this is the only second "page" the app has) rendering `SharePage` instead of `App`; it polls every 15s (not a live realtime subscription -- deferred, polling is simpler and good enough for a first version), shows the sharer's name/photo via a join against `profiles`, and a "Get directions to here" button that hands off to the main app via URL params (`?destLat=&destLng=&destLabel=`) since there's no shared in-memory state between the two pages on a fresh load. Made this handoff actually routable: `staticRoute`'s destination-side node resolution previously assumed every destination was a real place with a precomputed `nearest_node_id` -- extended it to fall back to `nearestNodeId()` against the live coordinate the same way live-location origins already do, so an arbitrary shared lat/lng can be routed to, not just catalogued places. Extracted MapLibre's one-time protocol/worker setup out of `MapView.jsx` into `src/map/setupMapLibre.js` so `SharePage`'s own separate map instance doesn't duplicate or skip it. Added `vercel.json` with an SPA rewrite (everything except `/api/*` falls back to `index.html`) -- without it, a fresh/direct load of a shared link (not a client-side navigation) would 404 on Vercel's static hosting; this couldn't be tested locally since it's Vercel-specific routing behavior, flagging for the user to confirm post-deploy. Verified what's testable pre-deploy in a real browser: `/share/:id` correctly renders the separate SharePage (confirmed the main app's markers do NOT render on that route), the error/not-found state renders gracefully when the share doesn't exist (correctly resolves after a real ~4-6s Supabase round-trip, not instant -- initial 2s check was premature, not a bug), and the `?destLat=&destLng=&destLabel=` handoff correctly auto-opens Directions with the shared location pre-filled as the destination. `npm run lint`, `npm test`, and `npm run build` all pass. | **Needed**: (1) paste `supabase/migrations/20260810120000_location_shares.sql` into the Supabase SQL Editor. (2) After the next deploy, confirm a shared link actually works from a **fresh browser** hitting `https://ful-pathfinder.vercel.app/share/<id>` directly (not by clicking from within the app) -- that's the one thing this session's local dev server can't verify, since it depends on Vercel's rewrite config actually taking effect. (3) Sign in for real and try the full loop yourself: share your location from the You tab, open the link in another browser/incognito window, confirm the position and "Get directions to here" work end to end -- same reasoning as every other account-gated feature this session, a real share can't be created and cleaned up from here. Admin moderation panel is next.
